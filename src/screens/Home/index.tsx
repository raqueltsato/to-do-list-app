import Card from "../../components/Card";
import { Container } from "./styles";

const Home = (): React.ReactNode => {
  return (
    <Container>
      <Card />
      <Card />
    </Container>
  );
};

export default Home;
